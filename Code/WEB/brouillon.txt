function setup(){
    createCanvas(windowWidth, windowHeight, WEBGL);
    background(0);
    // affichage du soleil
    let sun = createImg(
        '/assets/soleil.png'
    );
    sun.position(25, 10);
    sun.size(250, 250);

    //affichage de la terre
    let earth = createImg(
        '/assets/earth.png'
    );
    earth.position(1000, 10);
    earth.size(270, 250);
}