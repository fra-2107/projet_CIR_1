function setup(){
    createCanvas(windowWidth, windowHeight, WEBGL);
    background(0);
    // affichage du soleil
    let sun = createImg(
        '/assets/soleil.png'
    );
    sun.position(25, 10);
    sun.size(250, 250);

    //affichage de la terre
    let earth = createImg(
        '/assets/earth.png'
    );
    earth.position(1000, 10);
    earth.size(270, 250);
}


open json ? :
,{
        method: "PUT",
        headers : {
            "Access-Control-Allow-Origin": "*",
            "Access-Control-Allow-Methods": "POST, PUT, GET, OPTIONS",
            "Access-Control-Allow-Headers": "Origin, X-Requested-With, Content-Type, Accept, Authorization"
        }
    }


async function demande(planete, methode, adresse){

    adresse=String(adresse)
    console.log(typeof(adresse))
    //ouverture du JSON
    const reponse= await fetch(adresse);
    const fichier= await reponse.json();

    planete = fichier[methode];

    p=echelle(planete)
    
    return p